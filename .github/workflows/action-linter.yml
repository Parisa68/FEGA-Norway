name: Lint GitHub Actions

on:
  pull_request:
    paths:
      - '.github/workflows/**'

jobs:
  lint:
    name: Run actionlint
    runs-on: ubuntu-latest

    steps:
      - name: Run actionlint
        uses: raven-actions/actionlint@v2
